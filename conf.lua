-- function love.conf(t)
--     t.window.title = "balls"
--     -- t.identity = "data/saves"
--     -- t.version = "1.0.0" -- versão do love pro jogo 
--     t.console = false

--     t.window.width = 500
--     t.window.height = 500
--     t.window.borderless = false -- não é adequado forçar aos usuarios, opicional    
--     t.window.vsync = 0 -- mesmo caso do borderless
--     t.window.display = 1 -- em qual monitor o app vai abrir
--     t.window.fullscreen = false

    
--     -- define a posição onde a janela abre, não forçar no usuario, é usado para salvar a posição onde o usuario deixou a tela
--     -- t.window.x = 200
--     -- t.window.y = 200


--     -- t.externalstorage = true -- apenas para android
--     -- t.gammacorrect = true -- fica dispoivel caso a maquina do jogador tenha suporte (correção de luz e tals)
--     -- t.audio.mic = true -- permissão de uso de microfone, smarth phone provavelmente



--     -- t.window.resizable -- não muito usado por causar problemas e poder quebrar o jogo
--     -- caso use o resizable, lembrar de setar tamanho minimo e maximo 

--     -- t.window.minheight
--     -- t.window.minwidth
--     -- t.window.maxheight
--     -- t.window.maxwidth
    
-- end